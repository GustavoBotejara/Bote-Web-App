import React from 'react'
import ReactDOM from 'react-dom/client'
import Bote from './Bote.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Bote />
  </React.StrictMode>,
)
